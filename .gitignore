# Dependencias
node_modules/
.pnpm-store/

# Builds
dist/
build/
.turbo/
*.tsbuildinfo

# Remotion
.remotion/
packages/video/out/

# Datos locales (índice en Postgres; los binarios no van al repo)
library/assets/
# previews del brand kit: mp4/png regenerables con `pnpm previews:kit`, y se
# rehacen con cada cambio de marca. Son datos, no código.
library/builtin-previews/
outputs/
!outputs/.gitkeep
!library/assets/.gitkeep

# Entorno
.env
.env.local
.env.*.local

# Sistema
.DS_Store
*.log

# Docker
postgres-data/
redis-data/

# Modelos de embeddings descargados
.fastembed_cache/
local_cache/

# Banco de guiones: los CASOS y la rúbrica se versionan (son el conjunto de
# evaluación y el criterio); las CORRIDAS no, son salida y se rehacen en 3 min.
banco/corridas/
