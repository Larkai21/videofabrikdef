// TODO(agente brand kit): lista de componentes con estado, log y preview,
// subida de zips, activación por canal y versionado. SPEC §10.
export function Componentes() {
  return (
    <div className="wrap-1160" style={{ padding: 'calc(var(--pad) * 2) 26px' }}>
      <h1 className="head" style={{ fontSize: 26, letterSpacing: '-0.02em', margin: '0 0 6px' }}>
        Brand kit
      </h1>
      <p className="muted fs-sm" style={{ margin: 0 }}>
        La validación de componentes por zips llega en este sprint.
      </p>
    </div>
  );
}
